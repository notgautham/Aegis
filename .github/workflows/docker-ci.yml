name: Docker CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'docker/**'
      - 'frontend/**'
      - 'docker-compose.yml'
      - 'requirements.txt'
      - '.github/workflows/docker-ci.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'docker/**'
      - 'frontend/**'
      - 'docker-compose.yml'
      - 'requirements.txt'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Test Aegis image build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.oqs
          push: false
          tags: aegis:test
          cache-from: type=gha
          cache-to: type=gha,mode=max
